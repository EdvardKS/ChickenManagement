Objetivo: Desarrollar la funcionalidad completa para gestionar los encargos y el stock de un negocio de venta de pollos. Ya se cuenta con el frontend montado en React con TanStack Query y TailwindCSS, pero falta implementar la base de datos, las rutas API y la lógica de negocio.

🔹 1. Base de Datos
📌 Tabla: orders (Encargos)
Crea una tabla orders para registrar los pedidos con los siguientes campos:

id (UUID, PK, autogenerado)
customer_name (STRING, NOT NULL)
customer_phone (STRING, puede ser NULL)
details (TEXT, puede ser NULL)
order_date (TIMESTAMP, fecha de creación del encargo, autogenerada)
pickup_date (DATE, fecha en que el cliente recogerá el pedido)
pickup_time (TIME, hora en que el cliente recogerá el pedido)
quantity (FLOAT, almacena la cantidad de pollos, por ejemplo, 1.5 en lugar de 1 1/2)
is_delivered (BOOLEAN, default: false)
is_canceled (BOOLEAN, default: false)
is_manual_entry (BOOLEAN, default: true, indica si se registró desde el panel manualmente)
holiday_name (STRING, puede ser NULL, almacena el nombre del día festivo si aplica)
was_error (BOOLEAN, default: false, indica si se marcó como "Apuntado por Error")
📌 Tabla: stock (Control de Stock)
Crea una tabla stock para registrar la gestión del inventario:

id (UUID, PK, autogenerado)
date (DATE, NOT NULL, cada día se registra un nuevo stock)
total_mounted_chickens (INTEGER, cantidad de pollos montados al inicio del día)
current_stock (FLOAT, cantidad de pollos disponibles en tiempo real)
reserved_stock (FLOAT, cantidad de pollos con encargo)
unreserved_stock (FLOAT, cantidad de pollos sin encargo)
created_at (TIMESTAMP, default: now())
updated_at (TIMESTAMP, se actualiza con cada cambio)
📌 Tabla: stock_log (Registro de Acciones en Stock)
Cada vez que se modifique el stock, se guarda un historial de cambios:

id (UUID, PK, autogenerado)
date (DATE, NOT NULL)
action (STRING, "add", "remove", "sale", "reset", "error")
quantity (FLOAT, cantidad modificada)
previous_stock (FLOAT, stock antes de la modificación)
new_stock (FLOAT, stock después de la modificación)
order_id (UUID, puede ser NULL si no está relacionado con un pedido)
created_at (TIMESTAMP, default: now())
🔹 2. Backend (API en Node.js con Express y PostgreSQL)
Crea rutas API para gestionar los pedidos y el stock:

📌 Rutas para Encargos (/api/orders)
POST /api/orders → Crea un nuevo encargo en la base de datos.
GET /api/orders → Obtiene todos los encargos.
PATCH /api/orders/:id/delivered → Marca un encargo como entregado (is_delivered = true).
PATCH /api/orders/:id/canceled → Marca un encargo como cancelado (is_canceled = true).
PATCH /api/orders/:id/error → Marca un encargo como error (was_error = true).
DELETE /api/orders/:id → Elimina un encargo y actualiza el stock sumando la cantidad eliminada.
📌 Rutas para Stock (/api/stock)
GET /api/stock → Obtiene el stock actual del día.
POST /api/stock/add → Suma pollos al stock (+6, +1).
POST /api/stock/remove → Resta pollos del stock (-6, -1).
POST /api/stock/sell → Resta stock al vender sin encargo (-0.5, -1).
POST /api/stock/reset → Resetea los valores del día.
CRON Job a las 06:00 → Instancia automáticamente el stock a 0 cada día.
CRON Job a las 07:00 → Verifica que el stock fue reseteado correctamente y, si no, lo hace.
🔹 3. Frontend (React + TanStack Query)
📌 Modificar el Drawer de Nuevo Encargo
Enviar la data al endpoint /api/orders con:
Nombre del cliente
Teléfono
Fecha y hora de recogida
Cantidad de pollos en formato float
Indicar que es un registro manual (is_manual_entry = true)
📌 Modificar el Drawer de Stock
Hacer funcionales los botones de +6, +1, -6, -1, -0.5, +0.5 enviando datos a /api/stock/add y /api/stock/remove.
El botón de "Iniciar Día" se reemplaza por "Resetear Valores" con doble alerta de confirmación y estilos de peligro.
Al hacer clic en "Entregado" desde la tabla:
Se envía un PATCH /api/orders/:id/delivered
Se actualiza el stock.actual restando la cantidad entregada.
📌 Agregar Botones en la Tabla de Pedidos
Cada pedido tendrá:

Botón "Ver Detalles" → Abre un Drawer con el pedido en grande.
Botón "WhatsApp" (si tiene teléfono) → Genera dinámicamente un link:
perl
Copiar
Editar
https://wa.me/<customer_phone>?text=Hola%20<customer_name>,%20tu%20pedido%20de%20<quantity>%20pollos%20está%20listo.
Nota: Este botón tendrá un icono con "saltitos" si el pedido necesita confirmación.
Botón "Entregado" → Marca el pedido como entregado y descuenta el stock.
Botón "Cancelado" → Marca el pedido como cancelado.
Botón "Apuntado por Error" → Restaura el stock y marca el pedido como error.
🔹 4. Dashboard de Análisis para Administrador
Crear una página para mostrar analíticas de pedidos y stock:

📈 Encargos por día, semana y mes.
📊 Cuántos fueron manuales vs. cuántos llegaron por otro medio.
📊 Promedio de cantidad de pollos por pedido.
📊 Ventas con vs. sin encargo.
📊 Días festivos con más encargos.
📊 Historial de stock y ajustes manuales.
🔹 5. Extras y Validaciones
✅ Si un pedido es eliminado, se suma su cantidad al stock automáticamente.
✅ Si un pedido es marcado como error, también se restaura el stock y se registra en el log.
✅ Validaciones en el backend para evitar cantidades negativas en el stock.
✅ Logs de acciones en stock_log para rastrear todos los cambios.
✅ CRON Jobs para asegurar la correcta inicialización del stock diario.

📌 Conclusión
Este prompt es hiper detallado para que la IA genere toda la funcionalidad completa del sistema, desde la base de datos hasta la integración con el frontend. Con esto, la IA podrá construir el backend, frontend y la estructura necesaria sin errores ni ambigüedades.